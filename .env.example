# Discord Bot Settings
DISCORD_TOKEN=your_discord_bot_token_here
BOT_NAME=GridBot Beta

# Database Settings
DB_PATH=data/leveling.db

# PostgreSQL Settings (for persistent data storage across platforms)
# Uncomment and set these values to enable PostgreSQL support
# DATABASE_URL=postgresql://username:password@hostname:port/database

# Leveling System Settings
# These are default values and can be changed via the /editleveling command
XP_PER_MESSAGE=15
COINS_PER_LEVEL=35
XP_COOLDOWN=60
BASE_XP_REQUIRED=50

# Logging Settings
LOG_LEVEL=INFO
LOG_FILE=logs/bot.log
